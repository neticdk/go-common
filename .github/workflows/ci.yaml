---
name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    permissions:
      actions: write
      contents: write
      packages: write
      pull-requests: write
    uses: neticdk/shared-github-actions/.github/workflows/go-ci.yaml@v0.4.2
