---
name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    permissions:
      actions: write
      contents: write
      packages: write
      pull-requests: write
    uses: neticdk/shared-github-actions/.github/workflows/go-ci.yaml@5c09391f313157791291094ae41effe75cc0e295 # v0.2.1
